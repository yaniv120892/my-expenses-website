# Proof-of-work assets

Screenshots and captured output backing PRs #32, #33 and #34.

Every image was taken from a browser driven against the application running
that PR's branch, with a real Postgres behind it. This branch carries no
application code and is not intended to be merged — delete it once the PRs
are closed and the links are no longer needed.
